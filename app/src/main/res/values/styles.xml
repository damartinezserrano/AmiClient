<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="Widget.Design.TextInputLayout.CustomDesign" >
        <item name="android:elevation">@dimen/textfElevation</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="android:backgroundTint">@color/whiteOpacity</item>
        <item name="android:background">@drawable/textfieldshape</item>
    </style>

    <style name="Base.TextAppearance.AppCompat.Widget.Button.CustomButton">
        <item name="android:elevation">@dimen/textfElevation</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:background">@drawable/buttonshape</item>

    </style>
</resources>
